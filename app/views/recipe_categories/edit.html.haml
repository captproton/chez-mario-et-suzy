%h2=l :label_title_edit_recipe_category

= error_messages_for :recipe_category

- labelled_form_for(@recipe_category) do |f|
  = render :partial => 'form', :locals => { :f => f }
  %p= f.submit l(:label_button_update)

%p= link_to l(:label_link_back), recipe_categories_path
