%h2=l :label_title_ingredient_categories

- if flash[:notice]
  #notice
    %p= flash[:notice]

#ingredient_categories
  = render :partial => "ingredient_category", :collection => @ingredient_categories

- content_for :sidebar do
  %li= link_to l(:label_link_new_ingredient_category), new_ingredient_category_path
  %li= link_to l(:label_link_units), units_path
  %li= link_to l(:label_link_periods), periods_path
